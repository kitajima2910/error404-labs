import React from "react";

const Docs = () => {

    throw Error();
    // throw Error("error at docs");

    return (
        <>
            <div>Docs</div>
        </>
    );
};

export default Docs;
