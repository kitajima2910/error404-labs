# Fix DOM Warning: Add Autocomplete Attribute to Password Input

## Overview
The Home page console shows a warning: "Input elements should have autocomplete attributes (suggested: 'current-password')" for the password input field with placeholder "Nhập mã truy cập".

## Steps
1. Locate the input element in `src/components/Nav.astro` at lines 402-408.
2. Add the `autocomplete="current-password"` attribute to the `<input>` tag.
3. Verify the change resolves the console warning by reloading the page and checking DevTools console.

## Files to Modify
- `src/components/Nav.astro`: Add autocomplete attribute.

## Validation
- After adding the attribute, test the Home page to ensure the warning is gone.
- Ensure no other password inputs in the codebase lack autocomplete attributes (search for type="password" if needed).

## Risks
- Minimal risk; adding autocomplete improves accessibility and follows best practices without changing functionality.