# Plan: Change "12 tháng" to "12 cấp độ" on /game-roadmap page

## Goal
Update the terminology on the game roadmap page from "months" to "levels" to reflect 12 levels instead of 12 months.

## Analysis
- The page uses an array called `months` with 12 objects (levels 1-12).
- UI text refers to "Month X" and uses "tháng" in Vietnamese.
- Need to change variable names, UI strings, and meta descriptions.

## Steps
1. Rename `const months` to `const levels`.
2. Update `months.map` to `levels.map`.
3. Change meta description from "mỗi tháng 4 tuần" to "mỗi cấp độ 4 tuần".
4. Update `promptOf` function: "Tháng ${month}" to "Cấp độ ${month}".
5. Change grid header: 'Month ' + month.no to 'Cấp độ ' + month.no.
6. Ensure no other references to "month" in code; replace with "level" or "cấp độ" as appropriate.

## Files to Edit
- `src/pages/game-roadmap.astro`

## Validation
- Page loads without errors.
- UI shows "Cấp độ X" instead of "Month X".
- Prompts use "Cấp độ" instead of "Tháng".
- Meta description updated.</content>
<parameter name="filePath">D:\error404-labs\.kilo\plans\1778414813437-sunny-eagle.md