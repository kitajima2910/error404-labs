---
import { Image } from 'astro:assets'
import logomark from '../icons/logomark.svg'
import navData from '../data/navData'
import Link from './Link.astro'
import IconBlog from '../icons/IconBlog.astro'
import IconAbout from '../icons/IconAbout.astro'
import IconRSS from '../icons/IconRSS.astro'
import IconSearch from '../icons/IconSearch.astro'

const onIcons = (name: string) => {
    switch (name) {
        case 'Giới Thiệu':
            return { component: IconAbout }
        case 'Bài Viết':
            return { component: IconBlog }
        case 'RSS':
            return { component: IconRSS }
    }
}
---

<!-- <nav
    aria-label="Primary"
    class="bg-[#FBFBFB] shadow-[0_4px_12px_rgba(0,0,0,0.06)] min-h-25 flex justify-center items-center"
>
    <div class="flex items-center h-full w-10/12 mx-auto justify-between">
        <a href="/" class="logomark" arial-label="Go Home">
            <Image src={logomark} alt="logomark" class="rounded-full w-15" />
        </a>
        <ul class="flex">
            {
                navData.map((item) => (
                    <li class="mr-5">
                        <Link
                            text={item.name}
                            href={item.path}
                            style="primary"
                            isFilled={false}
                            borderVisible={true}
                            data-navLink
                            icon={{
                                component: onIcons(item.name)?.component,
                                side: 'left',
                            }}
                            target={item.name === 'RSS' ? '_blank' : '_self'}
                        />
                    </li>
                ))
            }
        </ul>
    </div>
</nav>

<script>
    import '../js/nav.js'
</script> -->
<nav
    class="fixed top-0 left-0 w-full z-50
    bg-[#FBFBFB]
    shadow-[0_4px_12px_rgba(0,0,0,0.06)]
    py-5"
>
    <div class="mx-auto w-10/12 px-0 sm:px-0 lg:px-0">
        <div class="flex h-16 items-center justify-between">
            <!-- Logo -->
            <div class="flex items-center">
                <a href="/" class="logomark" arial-label="Go Home">
                    <Image
                        src={logomark}
                        alt="logomark"
                        class="rounded-full w-20"
                    />
                </a>

                <!-- Open button -->
                <button id="openModal" class="ml-5">
                    <Link
                        text="Tìm"
                        href="#/tim"
                        style="primary"
                        isFilled={false}
                        borderVisible={true}
                        icon={{
                            component: IconSearch,
                            side: 'left',
                        }}
                    />
                </button>
            </div>

            <!-- Desktop Menu -->
            <div class="hidden md:flex items-center space-x-8">
                <ul class="flex">
                    {
                        navData.map((item) => (
                            <li class="ml-5">
                                <Link
                                    text={item.name}
                                    href={item.path}
                                    style="primary"
                                    isFilled={false}
                                    borderVisible={true}
                                    data-navLink
                                    icon={{
                                        component: onIcons(item.name)
                                            ?.component,
                                        side: 'left',
                                    }}
                                    target={
                                        item.name === 'RSS' ? '_blank' : '_self'
                                    }
                                />
                            </li>
                        ))
                    }
                </ul>
            </div>

            <!-- Mobile button -->
            <button
                id="menuBtn"
                class="md:hidden inline-flex items-center justify-center rounded-md p-3 text-blue-700 hover:bg-gray-100 focus:outline-none"
            >
                <svg
                    class="h-6 w-6"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                    viewBox="0 0 24 24"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        d="M4 6h16M4 12h16M4 18h16"></path>
                </svg>
            </button>
        </div>
    </div>

    <!-- Mobile Menu -->
    <div
        id="mobileMenu"
        class="md:hidden hidden border-t border-gray-200 mt-3.75"
    >
        <div class="space-y-1 px-4 py-4">
            <ul class="flex flex-col">
                {
                    navData.map((item) => (
                        <li class="mr-5">
                            <Link
                                text={item.name}
                                href={item.path}
                                style="primary"
                                isFilled={false}
                                borderVisible={!true}
                                data-navLink
                                icon={{
                                    component: onIcons(item.name)?.component,
                                    side: 'left',
                                }}
                                target={
                                    item.name === 'RSS' ? '_blank' : '_self'
                                }
                            />
                        </li>
                    ))
                }
            </ul>
        </div>
    </div>
</nav>

<!-- Overlay -->
<div
    id="modal"
    class="fixed inset-0 z-50 hidden items-center justify-center bg-black/50"
>
    <!-- Modal box -->
    <div
        class="w-full max-w-3xl m-10 rounded-2xl bg-white p-6 shadow-xl
           animate-in fade-in zoom-in duration-200"
    >
        <!-- Header -->
        <div class="flex items-center justify-between">
            <h2 class="text-lg font-semibold">
                Mọi Thứ Trong Website Ở Đây :D
            </h2>
            <button id="closeModal" class="text-gray-400 hover:text-gray-600">
                ✕
            </button>
        </div>

        <!-- Content -->
        <p class="mt-4 text-gray-600">
            <div class="max-h-[50vh] overflow-y-auto">
                <div id="search"></div>
            </div>
            <script>
                window.addEventListener('DOMContentLoaded', () => {
                    // @ts-ignore
                    new PagefindUI({
                        element: '#search',
                        debounceTimeoutMs: 300,
                        resetStyles: false,
                        showEmptyFilters: false,
                        excerptLength: 15,
                        showImages: !true,
                        showSubResults: true,
                        pageSize: 3,
                    })
                })
            </script>
        </p>

        <!-- Actions -->
        <div class="mt-6 flex justify-end gap-3 hidden">
            <button
                id="cancelModal"
                class="rounded-lg border px-4 py-2 hover:bg-gray-100"
            >
                Cancel
            </button>
            <button
                class="rounded-lg bg-indigo-600 px-4 py-2 text-white hover:bg-indigo-700"
            >
                Confirm
            </button>
        </div>
    </div>
</div>

<script>
    const btn = document.getElementById('menuBtn')
    const menu = document.getElementById('mobileMenu')

    if (btn && menu) {
        btn.addEventListener('click', () => {
            menu.classList.toggle('hidden')
        })
    }
</script>

<script is:inline>
    try {
        const modal = document.getElementById('modal')
        const openBtn = document.getElementById('openModal')
        const closeBtn = document.getElementById('closeModal')
        const cancelBtn = document.getElementById('cancelModal')

        const openModal = () => {
            modal.classList.remove('hidden')
            modal.classList.add('flex')
            document.body.classList.add('overflow-hidden')

            const search = document.querySelector('.pagefind-ui__search-input')
            const btnClear = document.querySelector(
                '.pagefind-ui__search-clear',
            )
            // console.log('search: ', search)
            if (search) {
                search.value = ''
            }

            if (btnClear) {
                btnClear.click()
            }
        }

        const closeModal = () => {
            modal.classList.add('hidden')
            modal.classList.remove('flex')
            document.body.classList.remove('overflow-hidden')
        }

        openBtn.onclick = openModal
        closeBtn.onclick = closeModal
        cancelBtn.onclick = closeModal

        modal.onclick = (e) => {
            if (e.target === modal) closeModal()
        }
    } catch (error) {}
</script>
