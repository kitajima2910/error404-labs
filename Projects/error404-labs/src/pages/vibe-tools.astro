---
import MainLayout from '../layouts/MainLayout.astro'
import { VIBE_PROMPTS } from '../data/vibe-tools/prompts.js'

export const prerender = false

// Serialize prompts to JSON for client-side use (content only, no server logic)
const promptsJson = JSON.stringify(
    VIBE_PROMPTS.map((p) => ({ id: p.id, toolName: p.toolName, content: p.content }))
)

const tools = [
    {
        id: 1,
        name: 'Calculator',
        desc: 'Máy tính mini đơn giản dễ sử dụng',
        icon: '🧮',
        bg: 'from-blue-400 to-blue-600',
    },
    {
        id: 2,
        name: 'Random Number',
        desc: 'Tạo số ngẫu nhiên may mắn',
        icon: '🎲',
        bg: 'from-pink-400 to-rose-500',
    },
    {
        id: 3,
        name: 'Random Emoji',
        desc: 'Tạo emoji ngẫu nhiên siêu vui',
        icon: '😊',
        bg: 'from-yellow-300 to-orange-400',
    },
    {
        id: 4,
        name: 'Dark Mode Toggle',
        desc: 'Chuyển đổi giao diện sáng / tối',
        icon: '🌙',
        bg: 'from-indigo-500 to-blue-800',
    },
    {
        id: 5,
        name: 'Button Sound',
        desc: 'Thêm âm thanh khi bấm nút',
        icon: '🔊',
        bg: 'from-teal-400 to-cyan-500',
    },
    {
        id: 6,
        name: 'Text Preview',
        desc: 'Xem trước văn bản trực tiếp',
        icon: 'Aa',
        bg: 'from-blue-400 to-indigo-500',
        isText: true,
    },
    {
        id: 7,
        name: 'Text Size Slider',
        desc: 'Điều chỉnh kích thước văn bản',
        icon: 'T',
        bg: 'from-orange-400 to-amber-500',
        isText: true,
    },
    {
        id: 8,
        name: 'Background Changer',
        desc: 'Thay đổi màu nền một cách dễ dàng',
        icon: '🎨',
        bg: 'from-purple-400 to-pink-500',
    },
    {
        id: 9,
        name: 'Click Counter',
        desc: 'Đếm số lần bạn click',
        icon: '👆',
        bg: 'from-teal-400 to-green-500',
    },
    {
        id: 10,
        name: 'Theme Picker',
        desc: 'Chọn giao diện bạn yêu thích',
        icon: '💝',
        bg: 'from-pink-400 to-rose-500',
    },
    {
        id: 11,
        name: 'Character Counter',
        desc: 'Đếm số ký tự trong văn bản',
        icon: '🔤',
        bg: 'from-green-400 to-emerald-500',
    },
    {
        id: 12,
        name: 'Word Counter',
        desc: 'Đếm số từ trong văn bản',
        icon: '📄',
        bg: 'from-blue-400 to-indigo-500',
    },
    {
        id: 13,
        name: 'Text Case Converter',
        desc: 'Chuyển đổi chữ hoa thường dễ dàng',
        icon: 'Aa',
        bg: 'from-red-400 to-orange-500',
        isText: true,
    },
    {
        id: 14,
        name: 'Text Trimmer',
        desc: 'Xóa khoảng trắng thừa trong text',
        icon: '✂️',
        bg: 'from-cyan-400 to-blue-500',
    },
    {
        id: 15,
        name: 'Text Reverser',
        desc: 'Đảo ngược văn bản',
        icon: '🔄',
        bg: 'from-teal-400 to-cyan-500',
    },
    {
        id: 16,
        name: 'To-Do List',
        desc: 'Quản lý công việc hàng ngày',
        icon: '✅',
        bg: 'from-red-400 to-pink-500',
    },
    {
        id: 17,
        name: 'Sticky Notes',
        desc: 'Ghi chú nhanh và lưu trữ',
        icon: '📝',
        bg: 'from-yellow-300 to-amber-400',
    },
    {
        id: 18,
        name: 'Countdown Timer',
        desc: 'Đếm ngược thời gian đơn giản',
        icon: '⏱️',
        bg: 'from-blue-500 to-indigo-600',
    },
    {
        id: 19,
        name: 'Pomodoro Timer',
        desc: 'Tập trung học tập hiệu quả',
        icon: '🍅',
        bg: 'from-red-400 to-rose-500',
    },
    {
        id: 20,
        name: 'Password Generator',
        desc: 'Tạo mật khẩu mạnh và an toàn',
        icon: '🔐',
        bg: 'from-gray-500 to-slate-700',
    },
    {
        id: 21,
        name: 'Color Picker',
        desc: 'Chọn màu sắc bạn yêu thích',
        icon: '🎨',
        bg: 'from-purple-400 to-pink-400',
    },
    {
        id: 22,
        name: 'Unit Converter',
        desc: 'Chuyển đổi đơn vị nhanh chóng',
        icon: '📏',
        bg: 'from-green-400 to-teal-500',
    },
    {
        id: 23,
        name: 'Date & Time',
        desc: 'Xem ngày giờ hiện tại',
        icon: '📅',
        bg: 'from-red-400 to-orange-500',
    },
    {
        id: 24,
        name: 'Stopwatch',
        desc: 'Đồng hồ bấm giờ chính xác',
        icon: '⏱️',
        bg: 'from-gray-400 to-slate-600',
    },
    {
        id: 25,
        name: 'Alarm Clock',
        desc: 'Đặt báo thức đơn giản',
        icon: '⏰',
        bg: 'from-orange-400 to-amber-500',
    },
]
---

<MainLayout title="50 Mini Tools Vibe | Error404-Labs" description="50 công cụ mini vibe giúp bạn học tập, làm việc và sáng tạo mỗi ngày!" searchable={false}>
    <script>
        async function checkAccess() {
            const token = localStorage.getItem('auth_token')
            if (!token) {
                window.location.href = '/?auth=required'
                return
            }
            try {
                const response = await fetch('/api/verify', {
                    headers: {
                        Authorization: `Bearer ${token}`,
                        'Content-Type': 'application/json',
                    },
                })
                const data = await response.json()
                if (!data.authenticated || !data.user) {
                    window.location.href = '/?auth=required'
                    return
                }
            } catch (error) {
                console.error('Access check failed:', error)
                window.location.href = '/?auth=required'
            }
        }
        checkAccess()
    </script>
    <div class="min-h-screen" style="background: linear-gradient(135deg, #f0f4ff 0%, #fdf0ff 50%, #fff0f8 100%);">

        <!-- Header -->
        <div class="max-w-6xl mx-auto px-4 pt-8 pb-4">

            <!-- Main Title -->
            <div class="text-center mb-6">
                <h1 class="text-4xl md:text-5xl font-extrabold mb-3 leading-tight">
                    <span class="text-yellow-400">✦</span>
                    <span class="text-gray-800"> 50 Mini Tools </span>
                    <span style="background: linear-gradient(135deg, #7c3aed, #ec4899); -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;">Vibe</span>
                    <span class="text-yellow-400"> ✦</span>
                </h1>
                <p class="text-gray-500 text-base md:text-lg">Những công cụ nhỏ xinh giúp bạn học tập, làm việc và sáng tạo mỗi ngày!</p>
            </div>

            <!-- Search Bar -->
            <div class="max-w-md mx-auto mb-8">
                <div class="relative">
                    <input
                        type="text"
                        id="toolSearch"
                        placeholder="Tìm kiếm tool bạn cần..."
                        class="w-full px-5 py-3 rounded-full border-2 border-purple-100 bg-white shadow-sm text-gray-700 placeholder-gray-400 outline-none focus:border-purple-300 transition-all text-sm"
                    />
                    <button class="absolute right-4 top-1/2 -translate-y-1/2 text-gray-400 hover:text-purple-500 transition-colors">
                        <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M21 21l-4.35-4.35M17 11A6 6 0 1 1 5 11a6 6 0 0 1 12 0z" />
                        </svg>
                    </button>
                </div>
            </div>
        </div>

        <!-- Tools Grid -->
        <div class="max-w-6xl mx-auto px-4 pb-12">
            <div id="toolsGrid" class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5 gap-5">
                {tools.map((tool) => (
                    <div
                        class="tool-card bg-white rounded-3xl p-5 shadow-sm border border-gray-100 flex flex-col items-center text-center transition-all duration-300 hover:-translate-y-1 hover:shadow-lg cursor-pointer"
                        data-name={tool.name.toLowerCase()}
                        data-desc={tool.desc.toLowerCase()}
                    >
                        <!-- Number badge -->
                        <div class="self-start mb-3">
                            <span class="text-xs font-bold text-gray-400 bg-gray-50 px-2 py-0.5 rounded-full">
                                {String(tool.id).padStart(2, '0')}
                            </span>
                        </div>

                        <!-- Icon -->
                        <div class={`w-16 h-16 rounded-2xl bg-gradient-to-br ${tool.bg} flex items-center justify-center mb-3 shadow-md`}>
                            {tool.isText ? (
                                <span class="text-white font-extrabold text-xl">{tool.icon}</span>
                            ) : (
                                <span class="text-3xl">{tool.icon}</span>
                            )}
                        </div>

                        <!-- Name & Desc -->
                        <h3 class="font-bold text-gray-800 text-sm mb-1 leading-tight">{tool.name}</h3>
                        <p class="text-gray-400 text-xs mb-4 leading-relaxed flex-1">{tool.desc}</p>

                        <!-- Buttons -->
                        <div class="w-full flex flex-col gap-2">
                            <button
                                class="w-full py-2 rounded-xl text-white text-xs font-semibold transition-all duration-200 hover:opacity-90 hover:scale-[1.02] active:scale-95 flex items-center justify-center gap-1.5"
                                style="background: linear-gradient(135deg, #7c3aed, #6d28d9);"
                                onclick={`window.vibeUseTool(${tool.id})`}
                            >
                                <span>▶</span>
                                <span>Dùng tool</span>
                            </button>
                            <button
                                class="w-full py-2 rounded-xl text-xs font-semibold transition-all duration-200 hover:opacity-90 hover:scale-[1.02] active:scale-95 flex items-center justify-center gap-1.5"
                                style="background: #fff0f8; color: #ec4899; border: 1.5px solid #fce7f3;"
                                onclick={`window.vibeViewPrompt(${tool.id})`}
                            >
                                <span>⊙</span>
                                <span>Xem prompt</span>
                            </button>
                        </div>
                    </div>
                ))}
            </div>

            <!-- No results message -->
            <div id="noResults" class="hidden text-center py-16">
                <div class="text-5xl mb-4">🔍</div>
                <p class="text-gray-500 text-lg font-medium">Không tìm thấy tool nào</p>
                <p class="text-gray-400 text-sm mt-1">Thử tìm kiếm với từ khóa khác nhé!</p>
            </div>

            <!-- View All Button -->
            <div class="flex justify-center mt-10">
                <button
                    class="px-8 py-3.5 rounded-full text-white font-bold text-sm shadow-lg transition-all duration-300 hover:shadow-xl hover:scale-105 active:scale-95 flex items-center gap-2"
                    style="background: linear-gradient(135deg, #7c3aed, #ec4899);"
                    id="viewAllBtn"
                >
                    <span>✦</span>
                    <span>Xem tất cả 50 tools</span>
                </button>
            </div>
        </div>
    </div>

    <!-- ===== PROMPT MODAL ===== -->
    <div
        id="promptModal"
        class="fixed inset-0 z-[9999] flex items-center justify-center p-4"
        style="display: none !important;"
        aria-modal="true"
        role="dialog"
        aria-labelledby="promptModalTitle"
    >
        <!-- Backdrop -->
        <div id="promptModalBackdrop" class="absolute inset-0" style="background: rgba(15, 10, 40, 0.65); backdrop-filter: blur(6px);"></div>

        <!-- Modal Panel -->
        <div class="relative w-full max-w-2xl max-h-[90vh] flex flex-col rounded-3xl overflow-hidden shadow-2xl" style="background: #fff;">

            <!-- Modal Header -->
            <div class="flex items-center justify-between px-6 py-5 flex-shrink-0" style="background: linear-gradient(135deg, #7c3aed 0%, #a855f7 50%, #ec4899 100%);">
                <div class="flex items-center gap-3">
                    <div class="w-9 h-9 rounded-xl bg-white/20 flex items-center justify-center text-lg">⊙</div>
                    <div>
                        <p class="text-white/70 text-xs font-medium uppercase tracking-wider">Prompt AI</p>
                        <h2 id="promptModalTitle" class="text-white font-bold text-base leading-tight"></h2>
                    </div>
                </div>
                <button
                    id="promptModalClose"
                    class="w-9 h-9 rounded-xl bg-white/20 hover:bg-white/30 flex items-center justify-center text-white transition-colors"
                    aria-label="Đóng"
                >
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12" />
                    </svg>
                </button>
            </div>

            <!-- Modal Body -->
            <div class="flex-1 overflow-y-auto px-6 py-5">
                <!-- Tip banner -->
                <div class="flex items-start gap-3 rounded-2xl p-4 mb-5" style="background: #fdf4ff; border: 1.5px solid #e9d5ff;">
                    <span class="text-purple-500 text-lg flex-shrink-0 mt-0.5">💡</span>
                    <p class="text-purple-700 text-xs leading-relaxed">
                        Copy prompt này và dán vào <strong>ChatGPT, Claude, Gemini</strong> hoặc bất kỳ AI nào bạn đang dùng để tạo ra tool hoàn chỉnh!
                    </p>
                </div>

                <!-- Prompt content -->
                <div class="relative">
                    <pre
                        id="promptContent"
                        class="w-full rounded-2xl p-5 text-sm leading-relaxed whitespace-pre-wrap break-words font-mono overflow-x-auto"
                        style="background: #f8f7ff; border: 1.5px solid #ede9fe; color: #3b0764; min-height: 120px;"
                    ></pre>
                </div>
            </div>

            <!-- Modal Footer -->
            <div class="flex items-center justify-between gap-3 px-6 py-4 flex-shrink-0" style="border-top: 1.5px solid #f3f4f6;">
                <p class="text-gray-400 text-xs">Dành riêng cho thành viên Vibe 💜</p>
                <button
                    id="promptCopyBtn"
                    class="flex items-center gap-2 px-5 py-2.5 rounded-xl text-white text-sm font-semibold transition-all duration-200 hover:opacity-90 hover:scale-[1.02] active:scale-95"
                    style="background: linear-gradient(135deg, #7c3aed, #ec4899);"
                >
                    <svg id="copyIcon" xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M8 16H6a2 2 0 01-2-2V6a2 2 0 012-2h8a2 2 0 012 2v2m-6 12h8a2 2 0 002-2v-8a2 2 0 00-2-2h-8a2 2 0 00-2 2v8a2 2 0 002 2z" />
                    </svg>
                    <svg id="checkIcon" xmlns="http://www.w3.org/2000/svg" class="w-4 h-4 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M5 13l4 4L19 7" />
                    </svg>
                    <span id="copyBtnText">Sao chép prompt</span>
                </button>
            </div>
        </div>
    </div>

    <!-- ===== AUTH GATE MODAL ===== -->
    <div
        id="authGateModal"
        class="fixed inset-0 z-[9999] flex items-center justify-center p-4"
        style="display: none !important;"
        aria-modal="true"
        role="dialog"
    >
        <!-- Backdrop -->
        <div id="authGateBackdrop" class="absolute inset-0" style="background: rgba(15, 10, 40, 0.65); backdrop-filter: blur(6px);"></div>

        <!-- Panel -->
        <div class="relative w-full max-w-sm rounded-3xl overflow-hidden shadow-2xl text-center" style="background: #fff;">
            <!-- Top gradient strip -->
            <div class="h-2 w-full" style="background: linear-gradient(90deg, #7c3aed, #ec4899);"></div>

            <div class="px-8 py-8">
                <!-- Lock icon -->
                <div class="w-16 h-16 rounded-2xl mx-auto mb-5 flex items-center justify-center text-3xl shadow-md" style="background: linear-gradient(135deg, #ede9fe, #fce7f3);">
                    🔒
                </div>

                <h3 class="font-extrabold text-gray-800 text-xl mb-2" id="authGateTitle">Nội dung dành riêng</h3>
                <p class="text-gray-500 text-sm leading-relaxed mb-6" id="authGateDesc">
                    Vui lòng <strong class="text-purple-600">đăng nhập</strong> để xem prompt AI dành cho thành viên Vibe.
                </p>

                <div class="flex flex-col gap-3">
                    <button
                        id="authGateLoginBtn"
                        class="w-full py-3 rounded-xl text-white font-bold text-sm transition-all duration-200 hover:opacity-90 hover:scale-[1.02] active:scale-95"
                        style="background: linear-gradient(135deg, #7c3aed, #ec4899);"
                    >
                        Đăng nhập ngay
                    </button>
                    <button
                        id="authGateCloseBtn"
                        class="w-full py-3 rounded-xl text-gray-500 font-semibold text-sm transition-all duration-200 hover:bg-gray-50"
                        style="border: 1.5px solid #e5e7eb;"
                    >
                        Để sau
                    </button>
                </div>
            </div>
        </div>
    </div>
</MainLayout>

<script define:vars={{ promptsJson }}>
    // ── Data ──────────────────────────────────────────────────────────────────
    /** @type {Array<{id: number, toolName: string, content: string}>} */
    const PROMPTS = JSON.parse(promptsJson)

    // ── Helpers ───────────────────────────────────────────────────────────────
    const isLoggedIn = () => !!localStorage.getItem('auth_token')

    /** Fetch the member's prompt_access list from the API (cached per session) */
    let _accessCache = null
    async function getMyAccess() {
        if (_accessCache !== null) return _accessCache
        try {
            const token = localStorage.getItem('auth_token')
            if (!token) { _accessCache = []; return _accessCache }
            const res = await fetch('/api/admin/prompt-access?action=my-access', {
                headers: { 'Authorization': `Bearer ${token}` }
            })
            if (res.ok) {
                const data = await res.json()
                _accessCache = data.access || []
            } else {
                _accessCache = []
            }
        } catch {
            _accessCache = []
        }
        return _accessCache
    }

    /** Check if the member has access to a specific vibe prompt */
    async function hasVibeAccess(id) {
        const access = await getMyAccess()
        return access.includes(`vibe_${id}`)
    }

    // ── Search ────────────────────────────────────────────────────────────────
    const searchInput = /** @type {HTMLInputElement} */ (document.getElementById('toolSearch'))
    const noResults = document.getElementById('noResults')
    const cards = /** @type {NodeListOf<HTMLElement>} */ (document.querySelectorAll('.tool-card'))

    searchInput?.addEventListener('input', () => {
        const query = searchInput.value.toLowerCase().trim()
        let visible = 0
        cards.forEach((card) => {
            const match = (card.dataset.name || '').includes(query) || (card.dataset.desc || '').includes(query)
            card.style.display = match ? '' : 'none'
            if (match) visible++
        })
        noResults?.classList.toggle('hidden', visible > 0)
    })

    // ── Use Tool ──────────────────────────────────────────────────────────────
    window.vibeUseTool = (id) => {
        if (window.showToast) {
            window.showToast(`Tool #${String(id).padStart(2, '0')} đang được phát triển! 🚀`, 'info')
        } else {
            alert(`Tool #${String(id).padStart(2, '0')} đang được phát triển! 🚀`)
        }
    }

    // ── Prompt Modal ──────────────────────────────────────────────────────────
    const promptModal = document.getElementById('promptModal')
    const promptModalTitle = document.getElementById('promptModalTitle')
    const promptContent = document.getElementById('promptContent')
    const promptModalClose = document.getElementById('promptModalClose')
    const promptModalBackdrop = document.getElementById('promptModalBackdrop')
    const promptCopyBtn = document.getElementById('promptCopyBtn')
    const copyIcon = document.getElementById('copyIcon')
    const checkIcon = document.getElementById('checkIcon')
    const copyBtnText = document.getElementById('copyBtnText')

    const openPromptModal = (id) => {
        const prompt = PROMPTS.find((p) => p.id === id)
        if (!prompt) return
        if (promptModalTitle) promptModalTitle.textContent = prompt.toolName
        if (promptContent) promptContent.textContent = prompt.content
        if (promptModal) {
            promptModal.style.removeProperty('display')
            promptModal.style.display = 'flex'
            // Reset copy button state
            resetCopyBtn()
            // Prevent body scroll
            document.body.style.overflow = 'hidden'
            // Focus close button for accessibility
            setTimeout(() => promptModalClose?.focus(), 50)
        }
    }

    const closePromptModal = () => {
        if (promptModal) {
            promptModal.style.display = 'none'
            document.body.style.overflow = ''
        }
    }

    const resetCopyBtn = () => {
        if (copyIcon) copyIcon.classList.remove('hidden')
        if (checkIcon) checkIcon.classList.add('hidden')
        if (copyBtnText) copyBtnText.textContent = 'Sao chép prompt'
        if (promptCopyBtn) promptCopyBtn.style.background = 'linear-gradient(135deg, #7c3aed, #ec4899)'
    }

    promptModalClose?.addEventListener('click', closePromptModal)
    promptModalBackdrop?.addEventListener('click', closePromptModal)

    promptCopyBtn?.addEventListener('click', async () => {
        const text = promptContent?.textContent || ''
        try {
            await navigator.clipboard.writeText(text)
            // Success state
            if (copyIcon) copyIcon.classList.add('hidden')
            if (checkIcon) checkIcon.classList.remove('hidden')
            if (copyBtnText) copyBtnText.textContent = 'Đã sao chép!'
            if (promptCopyBtn) promptCopyBtn.style.background = 'linear-gradient(135deg, #059669, #10b981)'
            setTimeout(resetCopyBtn, 2500)
        } catch {
            if (window.showToast) {
                window.showToast('Không thể sao chép. Hãy chọn và copy thủ công!', 'warning')
            }
        }
    })

    // Close on Escape key
    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') {
            closePromptModal()
            closeAuthGateModal()
        }
    })

    // ── Auth Gate Modal ───────────────────────────────────────────────────────
    const authGateModal = document.getElementById('authGateModal')
    const authGateBackdrop = document.getElementById('authGateBackdrop')
    const authGateLoginBtn = document.getElementById('authGateLoginBtn')
    const authGateCloseBtn = document.getElementById('authGateCloseBtn')

    const openAuthGateModal = (mode = 'login') => {
        // Update text based on mode
        const title = document.getElementById('authGateTitle')
        const desc = document.getElementById('authGateDesc')
        const loginBtn = document.getElementById('authGateLoginBtn')
        if (mode === 'no-access') {
            if (title) title.textContent = 'Chưa có quyền truy cập'
            if (desc) desc.innerHTML = 'Prompt này dành riêng cho thành viên được <strong class="text-purple-600">Admin cấp quyền</strong>. Hãy liên hệ Admin để được mở khóa.'
            if (loginBtn) loginBtn.style.display = 'none'
        } else {
            if (title) title.textContent = 'Nội dung dành riêng'
            if (desc) desc.innerHTML = 'Vui lòng <strong class="text-purple-600">đăng nhập</strong> để xem prompt AI dành cho thành viên Vibe.'
            if (loginBtn) loginBtn.style.display = ''
        }
        if (authGateModal) {
            authGateModal.style.removeProperty('display')
            authGateModal.style.display = 'flex'
            document.body.style.overflow = 'hidden'
            setTimeout(() => (mode === 'no-access' ? authGateCloseBtn?.focus() : authGateLoginBtn?.focus()), 50)
        }
    }

    const closeAuthGateModal = () => {
        if (authGateModal) {
            authGateModal.style.display = 'none'
            document.body.style.overflow = ''
        }
    }

    authGateBackdrop?.addEventListener('click', closeAuthGateModal)
    authGateCloseBtn?.addEventListener('click', closeAuthGateModal)

    authGateLoginBtn?.addEventListener('click', () => {
        closeAuthGateModal()
        // Trigger the existing login modal in Nav
        const openLoginBtn = document.getElementById('openLoginModal')
        if (openLoginBtn) {
            openLoginBtn.click()
        } else {
            window.location.href = '/dang-nhap'
        }
    })

    // ── View Prompt (auth-gated entry point) ──────────────────────────────────
    window.vibeViewPrompt = async (id) => {
        if (!isLoggedIn()) {
            openAuthGateModal()
            return
        }
        const allowed = await hasVibeAccess(id)
        if (allowed) {
            openPromptModal(id)
        } else {
            // Logged in but no access to this specific prompt
            openAuthGateModal('no-access')
        }
    }

    // ── View All Button ───────────────────────────────────────────────────────
    document.getElementById('viewAllBtn')?.addEventListener('click', () => {
        if (window.showToast) {
            window.showToast('25 tools tiếp theo đang được phát triển! Hãy chờ đón nhé 🍄', 'info')
        } else {
            alert('25 tools tiếp theo đang được phát triển! Hãy chờ đón nhé 🍄')
        }
    })
</script>

<style>
    /* Smooth card hover */
    .tool-card {
        will-change: transform;
    }

    /* Search input focus ring */
    #toolSearch:focus {
        box-shadow: 0 0 0 3px rgba(124, 58, 237, 0.15);
    }

    /* Modal entrance animation */
    #promptModal > div:last-child,
    #authGateModal > div:last-child {
        animation: modalPop 0.25s cubic-bezier(0.34, 1.56, 0.64, 1) both;
    }

    @keyframes modalPop {
        from {
            opacity: 0;
            transform: scale(0.92) translateY(12px);
        }
        to {
            opacity: 1;
            transform: scale(1) translateY(0);
        }
    }

    /* Prompt content scrollbar */
    #promptContent::-webkit-scrollbar {
        width: 6px;
        height: 6px;
    }
    #promptContent::-webkit-scrollbar-track {
        background: transparent;
    }
    #promptContent::-webkit-scrollbar-thumb {
        background: #c4b5fd;
        border-radius: 99px;
    }
</style>
